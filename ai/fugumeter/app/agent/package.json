{
  "name": "fugumeter-agent",
  "version": "0.1.0",
  "license": "GPL-3.0-or-later",
  "private": true,
  "description": "Fugu Meter: pays per call, per second, per unit, with no human approving each one.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.scripts.json"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^5.0.0"
  }
}
